export interface Product {
    name: string;
    price: number;
    description: string
}
export interface Review {
    user: string;
    comment: string;
}
export interface QA {
    question: string;
    answer: string;
}